import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.drag.wallet',
  appName: 'Drag Wallet',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
